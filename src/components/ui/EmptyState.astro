---
interface Props {
  message?: string
  class?: string
}

const { message, class: className = "" } = Astro.props
---

<p
  class={`glass-card mx-auto max-w-xl p-6 text-center text-sm text-white/80 ${className}`}
>
  <slot>{message}</slot>
</p>
