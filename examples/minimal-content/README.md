# Minimal Content Template

This directory contains the smallest content set needed to start a forked Polyglow site:

- one English post
- one English About page
- one English author profile

Use it through:

```bash
pnpm theme:reset
pnpm theme:reset -- --apply
```

The first command is a dry run. The second copies the template into `src/content/`.
